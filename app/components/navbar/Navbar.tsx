import UpperNavbar from "./UpperNavbar";
import BottomNavbar from "./BottomNavbar";

const Navbar = () => {
  return (
    <div>
      <UpperNavbar />
      <BottomNavbar />
    </div>
  );
};

export default Navbar;
